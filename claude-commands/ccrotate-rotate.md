Smart-rotate to the next Claude Code account on standard (base) usage tier.

Run `ccrotate next` which tests each candidate and picks the first on standard tier.

After switching, remind the user to restart Claude Code to apply the account change.
